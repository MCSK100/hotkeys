# Copy to .env.local and fill in your keys
# Realtime race server
NEXT_PUBLIC_WS_URL=ws://localhost:3001
# GIF search in room chat — free key from Google Tenor API (see README note below)
NEXT_PUBLIC_TENOR_API_KEY=
